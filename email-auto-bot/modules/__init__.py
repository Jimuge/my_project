# -*- coding: utf-8 -*-
"""
让 Python 把 `modules` 文件夹当作一个包（package）

有了这个文件，就可以这样导入：
  from modules.config import Config
  from modules.email_watcher import EmailWatcher
  而不是用复杂的相对路径导入
"""

# 这个文件可以为空，存在就行
